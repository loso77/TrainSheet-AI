/* TrainSheet AI Version: 0.2.0 */
const cameraInput=document.getElementById("cameraInput"),galleryInput=document.getElementById("galleryInput"),previewCard=document.getElementById("previewCard"),previewImage=document.getElementById("previewImage"),startBtn=document.getElementById("startBtn"),clearBtn=document.getElementById("clearBtn"),statusText=document.getElementById("statusText"),workflowState=document.getElementById("workflowState"),resultBody=document.getElementById("resultBody"),todayCount=document.getElementById("todayCount"),needConfirmCount=document.getElementById("needConfirmCount"),devToggle=document.getElementById("devToggle"),debugPanel=document.getElementById("debugPanel"),demoBtn=document.getElementById("demoBtn");let currentImageUrl=null,devMode=false;
function handleImage(file){if(!file)return;if(currentImageUrl)URL.revokeObjectURL(currentImageUrl);currentImageUrl=URL.createObjectURL(file);previewImage.src=currentImageUrl;previewCard.classList.remove("hidden");startBtn.disabled=false;setWorkflow("照片已载入","photo");statusText.textContent="照片已载入。点击“开始处理”进入模拟识别流程。";clearResults()}
cameraInput.addEventListener("change",()=>handleImage(cameraInput.files[0]));galleryInput.addEventListener("change",()=>handleImage(galleryInput.files[0]));
clearBtn.addEventListener("click",()=>{if(currentImageUrl)URL.revokeObjectURL(currentImageUrl);currentImageUrl=null;previewImage.src="";previewCard.classList.add("hidden");startBtn.disabled=true;clearSteps();workflowState.textContent="等待照片";statusText.textContent="请先拍照或从相册选择一张完整图片。";clearResults()});
startBtn.addEventListener("click",async()=>{clearResults();startBtn.disabled=true;await runStep("photo","正在读取照片……");await runStep("paper","正在寻找整张纸……");await runStep("target","正在定位目标区域……");await runStep("ocr","正在识别车号和股道……");await runStep("confirm","正在检查是否需要人工确认……");workflowState.textContent="演示完成";statusText.textContent="V0.2.0 当前为流程演示版。下一版将开始加入真实图片定位。";todayCount.textContent="1";needConfirmCount.textContent="1";renderDemoResults();startBtn.disabled=false});
demoBtn.addEventListener("click",renderDemoResults);devToggle.addEventListener("click",()=>{devMode=!devMode;debugPanel.classList.toggle("hidden",!devMode);devToggle.textContent=devMode?"关闭开发模式":"开发模式"});
function setWorkflow(text,activeStep){workflowState.textContent=text;clearSteps();if(activeStep)document.querySelector(`[data-step="${activeStep}"]`)?.classList.add("active")}
function clearSteps(){document.querySelectorAll(".step").forEach(step=>step.classList.remove("active","done"))}
function markDone(stepName){const step=document.querySelector(`[data-step="${stepName}"]`);if(!step)return;step.classList.remove("active");step.classList.add("done")}
async function runStep(stepName,text){workflowState.textContent=text.replace("……","");statusText.textContent=text;document.querySelector(`[data-step="${stepName}"]`)?.classList.add("active");await sleep(600);markDone(stepName)}
function sleep(ms){return new Promise(resolve=>setTimeout(resolve,ms))}
function clearResults(){resultBody.innerHTML=`<tr class="empty-row"><td colspan="4">暂无结果</td></tr>`;needConfirmCount.textContent="0"}
function renderDemoResults(){resultBody.innerHTML=`<tr><td>固定时刻</td><td>04东</td><td>020</td><td class="ok">演示</td></tr><tr><td>固定时刻</td><td>18西</td><td>112</td><td class="warn">需确认</td></tr>`}
if("serviceWorker"in navigator){navigator.serviceWorker.register("sw.js").catch(()=>{})}
