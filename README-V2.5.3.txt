V2.5.3 动态配置中心

新增：
- 表号列表可以增加、删除、排序，不再写死31—61。
- 每个表号的时间可以修改，不再写死。
- 车号最小值、最大值和“是否允许重复”可以配置。
- 股道唯一性为永久核心规则，不能关闭。
- 配置保存在本机浏览器，并随每次识别请求传给后端。
- Excel导出会使用当前配置的时间，并适配记录数量。

部署：前端覆盖GitHub Pages；后端覆盖Cloudflare Pages；然后执行 migration-v2.5.3.sql 一次。
